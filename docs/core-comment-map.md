# Core System Comment Map (Deprecated)

เอกสารไฟล์นี้เลิกใช้งานแล้วเพื่อหลีกเลี่ยงข้อมูลซ้ำและเลขบรรทัดที่ล้าสมัย

โปรดใช้ไฟล์หลักแทน:
- docs/core-comment-map-v2.md

หากต้องอ้างอิงในการพรีเซนต์ ให้ใช้เวอร์ชัน v2 เป็นแหล่งเดียว (single source of truth)
